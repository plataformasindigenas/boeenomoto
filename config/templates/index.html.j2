{% extends "base.html.j2" %}

{% block title %}Início{% endblock %}

{% block extra_styles %}
        .hero {
            text-align: center;
            padding: 2.5rem 1rem;
            background: #3D352F;
            color: white;
            border-radius: 4px;
            margin-bottom: 2rem;
        }
        .hero h1 {
            color: white;
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }
        .hero p {
            font-size: 1.2rem;
            opacity: 0.85;
            max-width: 600px;
            margin: 0 auto;
        }
        .platforms {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        .platform-card {
            background: white;
            border-radius: 4px;
            padding: 1.5rem;
            border: 1px solid #E8E4DF;
            text-decoration: none;
            color: inherit;
            transition: border-color 0.2s;
            display: block;
        }
        .platform-card:hover {
            border-color: #C75B39;
        }
        .platform-card h2 {
            margin: 0 0 0.5rem 0;
            color: #3D352F;
            font-size: 1.4rem;
        }
        .platform-card p {
            margin: 0 0 1rem 0;
            color: #666;
        }
        .platform-card .count {
            font-size: 2rem;
            font-weight: 700;
            color: #C75B39;
        }
        .platform-card .label {
            font-size: 0.9rem;
            color: #888;
        }
        .about {
            background: white;
            border-radius: 4px;
            padding: 1.5rem;
            border: 1px solid #E8E4DF;
        }
        .about h2 {
            margin-top: 0;
            color: #3D352F;
        }
{% endblock %}

{% block content %}
    <div class="hero">
        <h1>Boe Eno Moto</h1>
        <p>Mundo Bororo</p>
    </div>

    <div class="platforms">
        <a href="dictionary.html" class="platform-card">
            <h2>Dicionário</h2>
            <p>Palavras e expressões da língua Bororo com tradução para Português, transcrição fonética e exemplos.</p>
            <div class="count">{{ data[0].dictionary_count }}</div>
            <div class="label">entradas</div>
        </a>

        <a href="fauna.html" class="platform-card">
            <h2>Fauna</h2>
            <p>Nomes de animais na língua Bororo com classificação tradicional, nomes científicos e imagens.</p>
            <div class="count">{{ data[0].fauna_count }}</div>
            <div class="label">espécies</div>
        </a>
    </div>

    <div class="about">
        <h2>Sobre o Projeto</h2>
        <p>
            <strong>Boe eno moto</strong> ("mundo Bororo") é uma plataforma dedicada à preservação,
            documentação e valorização dos saberes linguísticos e culturais do povo Bororo.
        </p>
        <p>
            Este repositório reúne uma variedade de recursos linguísticos e etnográficos, tanto
            históricos quanto contemporâneos, com o objetivo de apoiar o ensino, a pesquisa e os
            esforços de revitalização da língua Bororo. A plataforma oferece um conjunto de
            ferramentas voltadas para o estudo linguístico e antropológico.
        </p>
    </div>
{% endblock %}
